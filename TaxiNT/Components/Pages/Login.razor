@page "/login"
@layout EmptyMainLayout
@inject IJSRuntime jS
@rendermode @(new InteractiveServerRenderMode(prerender: true))

<SeoHeaderComponent Title="Đăng nhập - Taxi Nam Thắng"
                    Description="@($"Login")" />

<PageTitle>Đăng nhập</PageTitle>

<AuthorizedContent >
    <ChildContent>
        <RedirectToDashboard />
    </ChildContent>
    <NotAuthorizedContent>
        <LoginComponent  />
    </NotAuthorizedContent>
</AuthorizedContent>
